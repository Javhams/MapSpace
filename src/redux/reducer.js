import { PARTNERS } from '../shared/partners';
import { DESK } from '../shared/desk';

export const initialState = {
    partners: PARTNERS,
    desk: DESK
    
};
//promotions: PROMOTIONS
export const Reducer = (state = initialState, action) => {
    return state;
};